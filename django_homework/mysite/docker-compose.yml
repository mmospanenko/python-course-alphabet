version: "3"

services:
  server:
    build: .
    env_file:
      - .env
    command: python manage.py runserver 0.0.0.0:8000 --noreload
    volumes:
      - .:/code
      - media:/media
    ports:
      - "80:8000"
    expose:
      - '80'

volumes:
  media: